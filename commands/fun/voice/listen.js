const djs = require("discord.js");

module.exports = {
    data: {
        name: "listen",
        description: "Have the bot listen to you"
    },
    async execute(context) {
        context.message.reply("Not implemented yet. xacer will work on it soon:tm:");
    }
};